import { Box, Container, Typography } from '@mui/material'
import React from 'react'

const EmailInbox = (props) => {
    const {...others} = props
  return (
    <>
      <Box {...others} sx={{display: "flex", flexDirection: "columns"}}>
      
      </Box>
    </>
  )
}

export default EmailInbox
